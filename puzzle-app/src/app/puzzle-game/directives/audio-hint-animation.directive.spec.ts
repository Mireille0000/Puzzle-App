import { AudioHintAnimationDirective } from './audio-hint-animation.directive';

describe('AudioHintAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioHintAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
