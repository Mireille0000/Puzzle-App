import { BackgroundImageDirective } from './set-background-image.directive';

describe('SetBackgroundImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImageDirective();
    expect(directive).toBeTruthy();
  });
});
