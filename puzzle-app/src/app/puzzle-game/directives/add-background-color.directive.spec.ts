import { BackgroundColorDirective } from './add-background-color.directive';

describe('AddBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
