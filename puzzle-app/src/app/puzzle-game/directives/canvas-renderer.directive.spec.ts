import { CanvasRendererDirective } from './canvas-renderer.directive';

describe('CanvasRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasRendererDirective();
    expect(directive).toBeTruthy();
  });
});
