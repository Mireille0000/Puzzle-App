import { SetBackgroundPositionDirective } from './set-background-position.directive';

describe('SetBackgroundPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackgroundPositionDirective();
    expect(directive).toBeTruthy();
  });
});
